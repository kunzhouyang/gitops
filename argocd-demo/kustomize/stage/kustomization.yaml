apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../base

namePrefix: stage-
nameSuffix: -v1

Labels:
- pairs:
    app: myapp
    env: stage
commonAnnotations:
  description: "stage env"

namespace: stage

images:
- name: myapp
  newName: easzlab.io.local:5000/myapp
  newTag: v1

configMapGenerator:
- name: myapp-cfg
  literals:
  - HAS_BUG=false

replicas:
- name: myapp
  count: 1

    #patches:
        #- patch: |-
        #   - op: replace
        #     path: /spec/replicas
        #     value: 3
        #  target:
        #    kind: Deployment
        #    name: myapp
